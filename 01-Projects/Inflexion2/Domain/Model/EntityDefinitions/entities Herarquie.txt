Entity is a class with an object value wich is  used to unique identify the entity
so two entities are the same if their id are de same.

From the entity we derives a validetable entity.

from validetable entity is derived an auditable entity which add another objet value to hold audit information wich his names is audit value.

from the auditable entity we derived a business entity which has a property to indicate the active state of the entity.

other class of entity es legacyEntity.


 