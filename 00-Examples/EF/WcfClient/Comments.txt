Este proyecto se ha creado con la plantilla de WCF library.
En este proyecto creamos el código de cliente de los servicios de WCF. 

Este proyecto debe referenciar aquellos proyectos que se vayan a necesitar para reutilizar.
En este caso es necesario que referencien los proyectos que contienen los DTOs. 
Por esta razón los poryetos de Dtos deberian estar separados.

Importante:Tambien es necesario incluir como referencia aquellas dependencias que tengan los proyectos de Dto.
Con objeto de que el generador del cliente incluya la reutilización de las clases de estos proyectos. En caso contario la compilación del reference.cs dara errores.
o las clases que despues lo referencien.



Al crear y ejecutar este proyecto en modo debugging es posible obtengamos una advertencia del tipo

"The target assembly contains no service types.  You may need to adjust the Code Access Security policy of this assembly."

It might happen when you run application at debug time. It is highly annoying and time consuming (it pauses application for more than one minute without any apparent reason). But what does it mean and why does it happen?

When you create a project from a WCF project templates Visual Studio knows that this project is a WCF service and thus it offers two debug time helpers: WCF Service Host and WCF Client. These two guys are intended to help you with running and testing WCF services without writing any code - they just appear at debug time. So far so good. But why the annoying dialog?

The dialog in question means that you have a project, created using one of the WCF project templates, with an interface marked with ServiceContract attribute and in the same project you don't have a class that implements this interface (perhaps you implemented that interface in another project). So, the WCF Service Host can't find a suitable class to host the service and it complains through that dreaded dialog box. Note that WCF Service Host is pretty dumb and it is incapable of searching through other projects in same solution. OK, the solution is to stop running WCF Service Host or even better, instruct it which class implements the interface in question. Well, AFAIK the later is impossible while the former can be done through project file modification using notepad. Here is how:

Delete this line from your project file:

<ProjectTypeGuids>{3D9AD99F-2412-4246-B90B-4EAA41C64699};{FAE04EC0-301F-11D3-BF4B-00C04F79EFBC}</ProjectTypeGuids>
otra forma de eliminar este error es ir a propiedades del proyecto. Tab de WCF options y quitar el check correspondiente a la opción:

Solution:
Open Visual Studio Command prompt and execute the following command:
sn -Vr "c:Program FilesMicrosoft SDKsWindowsv6.0ABinSvcUtil.exe"

NOTE:
Replace c:Program Files with the appriopriate path on your machine.

In additional, verify that there is no project within your solution that has the check box marked with :
Start WCF Service Host when debugging another project in the same solution
selected. The check box is found within the WCF Option.

http://cs.rthand.com/blogs/blog_with_righthand/archive/2008/03/10/_2200_The-target-assembly-contains-no-service-types.--You-may-need-to-adjust-the-Code-Access-Security-policy-of-this-assembly_2E002200_-annoyance.aspx

http://technoarkay.blogspot.com.es/2009/03/wcf-target-assembly-contains-no-service.http://ekhorutomwen.wordpress.com/2009/06/05/microsoft-wcf-service-host/


